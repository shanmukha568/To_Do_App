specVersion: "v1"
serviceId: "todoapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "todoapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
